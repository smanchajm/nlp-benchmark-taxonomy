model:
  pretrained: "allenai/scibert_scivocab_uncased"
  num_labels: 2
  max_length: 512
  output_dir: "data/models/scibert"
  num_train_epochs: 3
  per_device_train_batch_size: 16
  per_device_eval_batch_size: 32
  learning_rate: 2.0e-5
  weight_decay: 0.01
  warmup_ratio: 0.1
  seed: 42
